# Copy to .env (gitignored) — never commit real keys.
#   cp .env.example .env
# In GitHub Actions this comes from a repository secret, not a file.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the extraction model.
# BBP_MODEL=claude-sonnet-4-5
